[algokit]
min_version = "v2.0.0"

[project]
type = "contract"
name = "campustrust-blockchain"

[project.deploy]
command = "python -m contracts.deploy --deploy"

[project.compile]
command = "python -m contracts.deploy --compile"
